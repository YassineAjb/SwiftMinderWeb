<form method="post" action="{{ path('app_vote_delete', {'idv': vote.idv}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vote.idv) }}">
    <button class="btn">Delete</button>
</form>
